import React, { Fragment } from 'react';
import {Container} from 'react-bootstrap';

const Contacts = () => {
    return (
        <Fragment>
            <Container>
                <h1>Hello There Contacts!</h1>
                <p>Trying out routing in React</p>
            </Container>
        </Fragment>
    )
}

export default Contacts